// Client-side API base configuration
// Set this to your backend origin if different from the page origin.
// Examples:
//   window.API_BASE = 'http://localhost:3001';
//   window.API_BASE = 'https://interns-api.example.com';
// By default, uses relative paths (same origin).
window.API_BASE = window.API_BASE || '';

